// features/addresses/constants/addresses.ts
export const ADDRESS_LABEL_MAX = 80;
export const ADDRESS_TEXT_MIN = 5;

/** Default map center (Damascus) — adjust per market */
export const DEFAULT_MAP_CENTER = {
  latitude: 33.5138,
  longitude: 36.2765,
} as const;