export type HomeBanner = {
  id: string;
  title: string;
  subtitle?: string;
  imageUrl: string;
  actionUrl?: string;
};

export type Module = {
  id: string;
  name: string;
  iconUrl?: string;
};